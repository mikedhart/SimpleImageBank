<%= form_for(@image_bank) do |f| %>
  <% if @image_bank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_bank.errors.count, "error") %> prohibited this image_bank from being saved:</h2>

      <ul>
      <% @image_bank.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_bank_name %><br />
    <%= f.text_field :image_bank_name %>
  </div>
  <div class="field">
    <%= f.label :image_bank_description %><br />
    <%= f.text_area :image_bank_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
